{{ form_start(selectForm) }}
<div class="form-group mt-5">
	<h3 id="audioFont" class="mt-3 text-warning">Choisissez une adresse de FACTURATION :</h3>
	{{ form_row(selectForm.selectBillingAddress, {
            label: " "
        }) }}
</div>
<div class="form-group mt-5">
	<h3 id="audioFont" class="mt-3 text-warning">Choisissez une adresse de LIVRAISON :</h3>
	{{ form_row(selectForm.selectDeliveryAddress, {
            label: " "
        }) }}
</div>
<div class="form-group text-center mt-3">
	<button class="btn btn-warning border-light btn-lg mt-3 roboto Hl">{{ button_label|default('Enregistrez vos choix') }}</button>
</div>
{{ form_end(selectForm) }}
