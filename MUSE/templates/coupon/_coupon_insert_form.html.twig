{{ form_start(couponInsertform) }}
<div class="container">
	<div class="row justify-content-center">
		<div class="form-group d-flex pb-0">
            <i class="fa-solid fa-gift fa-lg mt-2 mr-1 surround"></i>{{ form_row(couponInsertform.code) }}
            {{ form_row(couponInsertform.submit, { 'label': 'Ajouter' }) }}
        </div>
    </div>
</div>
{{ form_end(couponInsertform) }}
